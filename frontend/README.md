# Frontend

The frontend will get the aggregated and raw emote data sent from server_a
and display the emote data.

# Testing with docker

# 1. Build and start the services
```bash
docker compose build
docker compose up &
```

# 2. Enter the frontend webpage
Go to browser and go to the page:
http://localhost:3000

The raw emote data will be logged in the console. 